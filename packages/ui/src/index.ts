// Shared React components live here once we have any.
// For now this package exports only the design-system tokens and Tailwind preset
// via the `./styles/tokens.css` and `./tailwind.preset` export entries.

export const TERIAC_UI_VERSION = "0.0.0";
